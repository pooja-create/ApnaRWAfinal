// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase:{
    apiKey: "AIzaSyBqithAc-xFiOhiX3kvgpq2vjtYQKxtctA",
    authDomain: "apnarwa.firebaseapp.com",
    databaseURL: "https://apnarwa.firebaseio.com",
    projectId: "apnarwa",
    storageBucket: "apnarwa.appspot.com",
    messagingSenderId: "193095246806",
    appId: "1:193095246806:web:5af78cd11796f55d823a77",
    measurementId: "G-TZGPXEDT9G"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
